# general settings
color cursor black green

set wrap-lines = yes

set line-graphics = utf-8

set tab-size = 4

set refresh-mode = auto

# vim like movement
# can't use g or gg - g is "grep"
bind diff b move-first-line
bind diff G move-last-line
