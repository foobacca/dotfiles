# ~/.tmuxinator/aptivate.yml
# you can make as many tabs as you wish...

name: Foobacca
root: ~/
pre: ssh-add
windows:
  - localmail:
      layout: even-horizontal
      panes:
        - #empty, will just run plain bash
        - mosh foobacca.co.uk -- tmux attach -d
